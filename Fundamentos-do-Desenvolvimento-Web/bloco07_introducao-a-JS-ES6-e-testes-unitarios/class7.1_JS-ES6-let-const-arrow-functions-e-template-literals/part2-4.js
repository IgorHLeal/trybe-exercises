const array = ['Git', 'JavaScrpit', 'CSS', 'HTML', 'C#'];

function functionOne (string) {
    const functionTwo = stringInner => (`Tryber ${stringInner} aqui!`);

    let result = `${functionTwo(string)} 
    
    Minhas cinco principais habilidades são`;

    // Continuar o código a partir daqui...
    
}